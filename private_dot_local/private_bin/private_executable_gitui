#!/bin/bash

H=$(date  "+%k")
DAWN=7
DUSK=19

# If time is between 7AM & 7PM
if (( DAWN <= H && DUSK > H )); then
    # Light theme
    gitui -t space_light.ron
else
    # Dark theme
    gitui -t space_dark.ron
fi
